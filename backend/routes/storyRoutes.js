import express from "express";

const router = express.Router();

router.get("/", (req,res) => {
    // GET requests

});

router.post("/", (req,res) => {
    // POST requests
    
});

export default router;